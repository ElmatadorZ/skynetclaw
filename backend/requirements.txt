# SkynetClaw / THE HOUSE — Python dependencies (Python 3.10+)
fastapi>=0.115.0
uvicorn>=0.30.6
httpx>=0.27.2
pydantic>=2.10.0
python-multipart>=0.0.9

# Optional: MCP bridge (backend/mcp_server.py). Remove if you don't use MCP.
mcp>=1.0.0 ; python_version >= "3.10"

# Dev / tests
pytest>=8.0.0
pytest-cov>=5.0.0

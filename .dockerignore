.git
.github
.venv
venv
**/__pycache__
**/*.py[cod]
.pytest_cache
.mypy_cache
.ruff_cache

# Never bake secrets or per-install state into an image
.env
.env.*
!.env.example
backend/settings.json
**/*.db
**/*.db-wal
**/*.db-shm
backend/sessions/
backend/memory/

# Large, not needed at runtime. backend/**/*.md is KEPT — the prompt layer,
# the skill registry and several subsystems read markdown at run time.
docs/
portraits/
SkynetClaw-Vault/
ui-debt/
